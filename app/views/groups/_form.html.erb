<%= form_for(@group) do |f| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% @group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :group_type_id %><br>
    <%= f.number_field :group_type_id %>
  </div>
  <div class="field">
    <%= f.label :total_users %><br>
    <%= f.number_field :total_users %>
  </div>
  <div class="field">
    <%= f.label :total_companies %><br>
    <%= f.number_field :total_companies %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
